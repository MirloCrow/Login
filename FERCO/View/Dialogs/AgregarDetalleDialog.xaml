<Window x:Class="FERCO.View.Dialogs.AgregarDetalleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FERCO.ViewModel"
        mc:Ignorable="d"
        Title="Agregar Producto"
        Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBackgroundBrush}">

    <Window.DataContext>
        <vm:AgregarDetalleDialogViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Título -->
            <RowDefinition Height="*"/>
            <!-- Contenido principal -->
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Agregar Producto"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{StaticResource PrimaryTextBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Contenido -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Selección de producto -->
            <StackPanel Margin="0,0,0,10">
                <TextBlock Text="Producto:" Foreground="{StaticResource PrimaryTextBrush}"/>
                <ComboBox ItemsSource="{Binding Productos}"
                          SelectedItem="{Binding ProductoSeleccionado}"
                          DisplayMemberPath="NombreProducto"
                          Width="400"/>
            </StackPanel>

            <!-- Cantidad y precio -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Cantidad:" VerticalAlignment="Center"
                           Margin="0,0,10,0"
                           Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBox Width="100" Text="{Binding Cantidad}"/>

                <TextBlock Text="Precio unitario:" VerticalAlignment="Center"
                           Margin="20,0,10,0"
                           Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBox Width="100" Text="{Binding PrecioUnitario}"/>
            </StackPanel>

            <!-- Botones -->
            <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Cancelar" Width="80" Margin="0,0,10,0"
                        Click="Cancelar_Click"
                        Style="{StaticResource BTNHEADERSTYLE}"/>
                <Button Content="Aceptar" Width="80"
                        Click="Aceptar_Click"
                        Style="{StaticResource BTNHEADERSTYLE}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
